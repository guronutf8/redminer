При запуске проферяется из сэйвов, есть ли положительный логин, т.е. autologin = true; B соотвестно не пусто логин, пасс и адрес;
    +запуск вервиса и выполнятеся активити со списком задач(TaskList)
    -активити ввода пасса

Ввод пасса
    +если залогинится полуалось, сэйвится настройки логина и autologin = true, открывается TaskList
    -autologin = false, отчиста в сэйвах найстрое логина, и отображение ошибки

TaskList
	+3.1 при заходе на страницу, запрашивается список задач(4.1)
	+3.2 при клике на задачу открывает(3.0) и передает в нее id и топик задачи

IssueActivity(3.0)


Сервис
	*4.1 Вытянутьс быза список задач и выдать их
	-4.2 (updateTasks) Обновиться список задач в базе данных(в цикле пишется в базу по одной задачи, надо перепилить что бы все разом писалось, а то реал букакэ) - закончил на том что пишется в базу =), и вообще вызывается из getTasks(переделать что бы вызывалось из 4.3. 
		-Не забыть запилиить что бы задачи все брались, а не только те что на главной странице
		-проработать таймауты, возможно сделать такое, если таймаут то следующий раз через 5 минут, ане пол часа.
	-4.3 зацикленый шудлер который запускает(4.2) список задач в базе
	---проблемма: при отправлении затраченого времени нет уверености что время проставилось, т.е.е нет подтверждения
	!При ошибке R.string.notify_error_send_time в title нужно прописывать имя задачи, иначе не однозначно






